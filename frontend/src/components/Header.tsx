import { AppBar, Toolbar } from "@mui/material";
import React from "react";

const Header = ()=>{
    return <>
        <AppBar sx={{}}>
            <Toolbar>

            </Toolbar>
        </AppBar>
    </>

};

export default Header;